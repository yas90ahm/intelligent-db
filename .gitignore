node_modules/
dist/

# logs / editor noise
*.log
.DS_Store
.vscode/
.idea/

# runtime db artifacts
*.db
*.db-wal
*.db-shm
*.sqlite

# EXCEPT checked-in test fixtures (small, intentionally versioned database files
# used by tests, e.g. the migration ladder's pre-ladder legacy fixture).
!src/__tests__/fixtures/*.db

# internal multi-agent run artifacts (kept local, not published) —
# Raw benchmark output stays local. The harness and concise reports are tracked.
.arbor/
.cursor/
ARBOR_CONTRACT.md
research_config.yaml
idb-rt/
run-full.ps1
